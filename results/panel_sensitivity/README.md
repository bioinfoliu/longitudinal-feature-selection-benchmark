# Panel-size sensitivity

The inner-validation table compares all candidate budgets (3, 5, 10). Outer-test and Jaccard tables are conditional on the budget selected inside each outer fold; they are not fixed-budget experiments. Strict fixed-budget outer-test curves require rerunning `scripts/run_fixed_budget_sensitivity.sbatch`.
